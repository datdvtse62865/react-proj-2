// import { selectStatisticPageDomain } from '../selectors';

describe('selectStatisticPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
